import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const GotCovid = () => {
    return (
        <View>
            <Text style={styles.text}>I got covid!!!!</Text>
        </View>
    );
};

const styles = StyleSheet.create({
    text: {},
});

export default GotCovid;
